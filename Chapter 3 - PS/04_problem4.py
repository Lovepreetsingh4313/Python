# Replace the double space from problem 3 with single spaces.

name = "Lovi is a good  boy"

print(name.replace("  ", " "))
print(name) # Strings are immutable which means that we cannot change them by running functions on them